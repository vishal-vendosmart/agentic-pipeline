# Agentic Pipeline - Python Dependencies (actual runtime deps only)

# HTTP client used by all agents (Linear GraphQL, DataForSEO Labs, Ollama Cloud, Telegram)
requests==2.31.0

# Testing
pytest==7.4.3
